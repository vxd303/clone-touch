# AutoTouch Lua docs access notes

Attempted to access the AutoTouch Lua documentation to compare available functions against the repository's feature set, but the network returned a 403 when trying to open the site from this environment.

## Commands and output

```bash
curl -L https://docs.autotouch.net/lua/
```
```
curl: (56) CONNECT tunnel failed, response 403
```

```bash
curl -L https://docs.autotouch.net
```
```
curl: (56) CONNECT tunnel failed, response 403
```
